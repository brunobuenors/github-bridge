{
  "name": "github-bridge",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "npx playwright install --with-deps && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.3.2",
    "playwright": "^1.42.1"
  }
}
